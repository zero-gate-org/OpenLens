const markup = `<div class="sidebar-panel" data-panel="stickers" id="stickers-sidebar-panel">
                  <div class="stk-layer-empty" style="padding:24px;text-align:center;color:var(--muted);font-size:0.72rem;">Load an image to start adding stickers.</div>
                </div>`;

export default markup;
