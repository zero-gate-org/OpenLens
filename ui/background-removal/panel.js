const markup = `<div class="sidebar-panel" data-panel="bgremove">
                  <label class="sidebar-field">
                    <span class="sidebar-label">Model</span>
                    <select id="bg-model" class="sidebar-select">
                      <option value="small">Small (fast)</option>
                      <option value="medium" selected>Medium</option>
                      <option value="large">Large (best)</option>
                    </select>
                  </label>
                  <button id="remove-background" class="btn btn-accent btn-full" type="button">Remove Background</button>
                </div>`;

export default markup;
